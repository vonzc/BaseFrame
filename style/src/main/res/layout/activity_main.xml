<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/btn_to_first"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:text="跳转到first模块的FirstActivity" />

    <Button
        android:id="@+id/btn_to_first_for_result"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btn_to_first"
        android:layout_centerVertical="true"
        android:text="跳转到first模块的FirstActivity,需要result" />

    <Button
        android:id="@+id/btn_to_kotlin_test"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btn_to_first_for_result"
        android:layout_centerVertical="true"
        android:text="跳转到kotlin1模块的KotlinFirstActivity" />

</RelativeLayout>